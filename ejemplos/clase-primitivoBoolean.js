// Primitivos Boolean

const isActive = true
const hasPermission = false

// Conversión implícita
const result = 5 > 3
console.log(result)

const name = 'Atigra Makers'
console.log(!!name);

// Conversión explícita
const value = 0
let explicitBoolean = Boolean(value)
console.log(explicitBoolean)

const otherValue = 24
explicitBoolean = Boolean(otherValue)
console.log(explicitBoolean)

